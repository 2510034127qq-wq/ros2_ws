# 项目介绍

现状核对日期：2026-09-14。运行代码基线：`244d067`。

## 项目定位

本项目是 ROS 2 Humble + Gazebo Classic 的静态持续热源巡检工作区。机器人探索环境，观察固定位置、持续发热的目标，将观测投影到地图，登记与去重热源，并尝试绕障接近和停留。

仿真使用 `g1_nav.urdf` 简化差速模型，接入 SLAM Toolbox 和 Nav2。完整 G1 URDF/mesh 保留为资产，当前没有人形步态或 G1 SDK 集成。项目另有面向外部 UGV 系统的热处理覆盖层，默认关闭运动。

任务持续运行到操作者停止或外部预算结束。当前不提供移动目标预测、生灭调度、预测重访或自动搜完判断。

## 当前系统组成

活动代码位于 `src/thermal_robot/`，包含 8 个 ROS 包：

| 包 | 当前职责 |
|---|---|
| `thermal_bringup` | 仿真/UGV 启动、参数、SLAM/Nav2、世界与 RViz 配置 |
| `g1_description` | 差速仿真模型与保留的 G1 资产 |
| `thermal_sensor_sim` | A 理想热场或 B 前视热表面/深度模拟 |
| `signal_preprocessor` | 热图预处理 |
| `thermal_field_reconstructor` | 场重建、测温输入、深度配准、透视投影与热地图融合 |
| `thermal_gradient_processor` | 热梯度与峰值信息 |
| `thermal_motion_controller` | 静态源登记、可选慢后验、探索与接近控制 |
| `thermal_interfaces` | 热场、热地图、源估计、后验消息与查询服务 |

热图预处理后分为两条支路：一条输出 `/thermal/field` 和 `/thermal/gradient`；另一条融合为 `/thermal/map`，再由快层生成 `/thermal/sources`。慢层订阅热地图和登记源，输出 `/thermal/belief`。控制器结合热信息、位姿、雷达与占据地图，通过 Nav2 action 或直接 `/cmd_vel` 控制运动。模拟真值只供评测，不参与目标估计或决策。

快层是唯一源身份入口。候选经过多帧确认成为 confirmed，已确认 ID 在本次会话内保留；缺测年龄增加不等于当前仍可见。慢层按登记 ID 更新后验，不独立创建身份。误确认不会自动撤销，身份和已处理状态尚不跨进程重启持久化。

## 默认观测与控制

- 主仿真默认 `sensor_model:=a strategy:=dual belief_mode:=online use_sim_time:=true`。
- A 级是 64×48、10 Hz 理想俯视热场；B 级是 160×120、8.6 Hz 前视热表面与深度，默认 passthrough 预处理。
- 默认 Config-B 是三个固定源的布局，与 B 级观测模型是不同概念；另有双源、五源、偏移源和软件契约静态场景。
- B 默认目标宽 0.3 m、高 0.8 m，表面温度在 28–37℃ 内按源 ID 和种子一次抽取后恒定，环境为 22℃；可通过 `source_temperature_range_c` 修改。A 级继续使用场景温升。
- `dual` 使用快层及健康、新鲜的 online 慢后验；`fast` 和 `gp_ucb` 不使用慢层反馈。shadow 仅估计，off 停止估计。
- B 级及 A + fast/dual/gp_ucb 走世界坐标接近/探索；只有 A + full/frontier/levy/residual 继续使用旧梯度状态机。
- 当前接近保持目标，受阻立即停车，持续受阻后选择已知安全停靠/中间点或延期。默认停距 1.2 m，局部推进步长上限 2 m。Nav2 结束不等于接近完成，还要检查位置、朝向、停留及确认状态。

仿真 `/odom` 使用 Gazebo 绝对世界位姿，出生点为 `(-6, 0)`；主 launch 已清零 mapper/controller 的出生偏移，不能重复加偏移。这是理想定位输入，不验证实际轮速漂移。ROS 仿真时钟与部分策略的单调墙钟计时并存。

参数位于 `thermal_bringup/config/`。仿真按 `params.yaml`、`software_params`（默认 `multisource.yaml`）、launch 内联参数覆盖；UGV 另加载 `hardware_params`（默认 `ugv_thermal.yaml`）。单独运行节点不等于加载主 launch 配置。

## 构建与启动

```bash
cd /home/hanchen/ros2_ws
source /opt/ros/humble/setup.bash
rosdep check --from-paths src/thermal_robot --ignore-src
colcon build --packages-select thermal_interfaces
source install/setup.bash
colcon build --packages-select g1_description thermal_sensor_sim signal_preprocessor thermal_field_reconstructor thermal_gradient_processor thermal_motion_controller thermal_bringup
source install/setup.bash
python3 -m pytest src/thermal_robot/tests/ -q
```

完整 GUI 仿真：

```bash
ros2 launch thermal_bringup sim_nav_slam_launch.py use_rviz:=true use_gzclient:=true
```

无界面 B 级仿真（与上面的仿真二选一）：

```bash
ros2 launch thermal_bringup sim_nav_slam_launch.py use_rviz:=false use_gzclient:=false sensor_model:=b
```

主仿真会启动 Gazebo、模型、SLAM/Nav2 和热链路。应等待节点与导航就绪后再采集；参数列表可用 `ros2 launch thermal_bringup sim_nav_slam_launch.py --show-args` 查看。正常停止用 Ctrl+C，残留 Gazebo 可用 `bash src/thermal_robot/kill_gz.sh` 清理。

## 数据与验证

算法测试在 `tests/`，采集和评测在 `scripts/`。`collect_sim_data.py` 为正在运行的仿真保存 CSV/JSON；`plot_all_figures.py` 和 `plot_slam_nav2.py` 读取实际采集目录出图。独立 A/B 闭环用 `run_software_validation.py`，静态多世界矩阵用 `run_multiscenario_matrix.py`；后者当前沿用 A 级观测。具体命令和指标见 [评测说明](docs/handover/04-评测体系与实验.md)。

本轮文档更新已执行 211 项纯算法测试，全部通过；没有重新启动仿真或进行实机验证。当前保留的闭环证据显示：三组 B 五源障碍场 180 秒采样均发现 5/5、物理接近 4/5；单次 300 秒达到 5/5。A 双源两次短跑仍为 0/2、1/2。因此当前不能保证任意场景和预算下搜齐、接近全部目标，也没有完成全矩阵统计性能验收。

## UGV 接入与能力边界

`ugv_thermal_launch.py` 默认 `enable_motion:=false use_sim_time:=false`，提供热处理与估计覆盖层；底盘、雷达、里程计、SLAM/导航及相关 TF 由外部系统负责。

真实定位要求有效测温图、CameraInfo、标定 optical TF 和配准深度。Lepton/PureThermal 本身不提供深度，普通 RGB/伪彩图也不等于温度图。当前有测温/深度/TF 的合成 ROS 输入契约检查，尚无真实相机标定、Pi 5 完整负载或实际车体运动验收。

## 当前说明入口

- [项目 README](src/thermal_robot/README.md)：快速启动、配置和验证命令。
- [现状报告](PROJECT_ANALYSIS_REPORT.md)：实现与证据边界。
- [交接导读](docs/handover/00-总览与导读.md)：架构、逐文件说明、控制流程、评测与排错。
- [UGV 运行文档](docs/software/multisource_runtime.md)：真实输入、参数与回放契约。

生成的 `build/`、`install/`、`log/`、`bags/` 和缓存不提交到 Git；原始实验数据应单独保存。
