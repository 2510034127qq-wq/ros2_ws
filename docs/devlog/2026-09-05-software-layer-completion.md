# 软件层重构交付

负责人范围：完成原规划的软件实现，允许缩减全量验证、基线和消融；真实硬件验证由负责人执行，论文门槛不作为本轮验收条件。

代码提交为 `ef20086`、`a2ac3ba`、`8ea46bf`。快层运动跟踪、默认源簇慢层、后验规划与清场、B 级三维表面/深度、硬件温度与深度接入、GP-UCB 均已进入 ROS 运行路径。主 launch 默认 dual/A/online 和仿真时钟，UGV 覆盖层默认禁止运动。

集成检查发现并修正了具体故障：B 图像时间滤波与移动深度错配导致伪热源；俯视足迹边缘被当作局部峰；占据图旋转/TF 转换遗漏；新控制路径漏接 Nav2 停滞回退；受阻源局部接近无限等待；fast 策略误接慢层先验；launch 将 off 解为布尔值；清场对多扇区漏检错误地先边缘化共享幅值。失败目录保留，未将其重新标成通过。

验证结果：173 项 pytest 测试通过，rosdep 通过，thermal_interfaces 和 7 个运行包构建成功，源码/install 56 对一致。A/B、动态物体、障碍生灭源、online/shadow/off、预算超时和 GP-UCB 共选取 7 个闭环通过记录；UGV 合成输入验证覆盖大端与行填充、温度/深度单位、完整光学 TF、跟踪确认与默认不输出运动。

最终 B 动态双层闭环 60 s：实际路径 9.92 m，2 个 confirmed 标签，60 次慢层 ready；快层更新最大 2.15 ms、规划最大 12.5 ms、慢层 p99 约 2.65 ms。预算注入检查连续 45 次 over_budget 时仍维持快层闭环。上述是本机软件证据，不是 Pi 5 实测或性能优越性证明。

完整需求对照、命令/数据路径、图像与限制见 [交付审计](../software/software_completion_audit.md)。真实温度标定、真实深度输入、车体参数和 Pi 5 性能留待实机；清场默认未校准且不触发自动结束。
