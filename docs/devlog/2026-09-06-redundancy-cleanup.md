# 2026-09-06 冗余清理

基于 `cb5f71e`，按用户“精简代码”的要求清理前次审计确认的冗余。保留静态探测、定位、去重、遮挡处理、导航、B 级观测、慢层和可运行基线。

## 代码变化

- 删除 5 个无效控制参数及其缓存、YAML 项和失效注释。
- 删除仅写未读的收敛中心、收敛角速度、找到源的时间、预峰值缓存、首次确认时间和三个节点的启动时间缓存；保留实际控制状态、TF/订阅生命周期句柄。
- 删除地图观测方向位图、方向时间历史、观测距离及对应 ROS 消息字段、配置和收发代码。`view_state`、遮挡计数和观测年龄保留。
- B 级慢层不再生成随后丢弃的高斯预测和残差场；A 级残差候选保持。
- 信息收益由控制器调用 `source_information_gain`，移除仅测试使用的重复方法。有效后验的定位/存在性收益和非有限协方差回退保持。
- 将 mapper 已声明但未传递的 `occupied_threshold` 接入占据视图，默认 65 不变。
- 清理无用导入；投影函数测试改为从定义模块 `observation` 导入。

运行代码 Python 物理行数 **8572 → 8507（−65）**，工具 **5700 → 5696（−4）**，另删 6 行配置和 2 个消息字段。新增行为回归测试，不以测试/文档行数充当生产代码减少量。

默认 200×200 网格：移除每格 float32 距离及 uint8 方向，约减少 **200,000 字节/帧**数组载荷；地图内部连同 8 方向 float32 时间历史共减少 **1,480,000 字节**常驻数组。忽略序列化对齐开销，未将其当作实测 CPU 或带宽提升。

## 验证

- `python3 -m pytest src/thermal_robot/tests/ -q`：**179 passed**。
- 61 个 Python 文件 AST 解析通过；`git diff --check` 通过。
- 构建 `thermal_interfaces`，再构建 7 个运行包：全部通过。
- 48 对源码/install 文件一致。
- B2 / dual / online / seed 101 / 90 秒：运行通过，2/2 物理源，0 重复时间桶、0 串 ID，匹配误差 P95 0.283 m，路径 8.86 m；慢层参与报告 53 次，P99 计算耗时 1.04 ms。
- A2 / dual / online / seed 101 / 90 秒：运行通过，但仅发现 1/2 物理源（漏 T2_east）；0 重复时间桶、0 串 ID，匹配误差 P95 0.118 m。保留漏检结果，不将链路通过解释为探测完整。
- UGV 合成输入接线：通过；1 个确认热源、17 条 ready 后验、0 运动输出。
- 新测试直接执行运行节点方法，覆盖信息收益解析值、多源累加、无效后验回退；非默认占据阈值改变通行判断；B 级跳过高斯预测且 A 级仍使用残差。
- 初次测试发现删除同一行的无用语句后遗留缩进，已修正；全部测试和语法检查随后通过。

运行结果及原始文件校验值见 `docs/software/redundancy_cleanup_validation.json`。原始日志保留在各 `bags/software_validation/20260906_redundancy_*` 目录（不入 Git）。首次 B2 启动传入了不存在的 `thermal_scene_open.world`，启动即失败；保留该目录，改用现有 `thermal_scene_nav.world` 重跑。

## 兼容范围

`ThermalMap` 删除 `view_sectors` 和 `last_view_distance_m`，外部消费者需重新构建；旧派生消息录包不保证兼容。此次是缩减回归验证，不替代全矩阵、真实相机、UGV 运动或 Pi 5 性能验证。上轮 A-fast 漏检、B5 长窗口标签不稳定等负面结果仍有效，本次清理不宣称解决这些问题。
