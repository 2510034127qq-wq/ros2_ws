# 探测恢复修复

授权：针对此前暴露的串 ID、熄灭后轨迹滞留与探索漏源执行软件修复，允许缩减验证。

实现：归一化关联评分、实测锚点门控、长期失观测速度衰减、当前冷观测反证、过期源规划约束清理、相机全周扫描、探索目标保持/受阻冷却、转向代价、地图启动就绪处理。新增离线物理身份匹配诊断，避免将 confirmed 标签数当作发现热源数。

验证：188 项纯 Python 检查通过，受影响两包构建通过，56 对源码/install 一致；A 静态、B 动态、B 障碍生灭三类代表场景短跑。保留中间失败结果，不重跑全量研究矩阵。结果与限制见 [探测修复记录](../software/detection_recovery.md)。

同时修正上一轮“原规划软件层全部完成”的过宽表述；未补齐的原规划条目在审计中明确列出。
