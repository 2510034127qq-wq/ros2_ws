# 软件重构交付与验收清单

依据：2026-06-10 总体设计及负责人 2026-09-05 最新授权。保留完整软件范围；缩减全量矩阵、基线与消融，取消论文显著性门槛和两周影子等待。实际硬件验证由负责人完成。旧实验目录不改动。

## 完成要求

- [x] 快层：常速 Kalman、预测关联、动态去重、重捕获记录、预测重访及探索仲裁。
- [x] 慢层：默认 Rao-Blackwell 源簇（运动条件高斯、强度/尺度估计、Bernoulli 存在概率和源数分布），残差 birth、可见性条件 death、merge/split/association、影子/在线/关闭、超时及异常回退。
- [x] 规划：残差与后验信息收益、可达性、重访、慢层先验校正；保持快层独立可运行。
- [x] 清场：显式空间/幅值/漏检模型、扇区和动态历史、快慢版本；校准输出与明确未校准状态。
- [x] B 级：三维盒/柱热表面、前视透视射线、遮挡/自遮挡、深度、噪声/偏移/emissivity；运行时算法不读取仿真热源真值。
- [x] 观测：时间/坐标系/完整外参/测量类型、热图与深度定位、A/B 可切换；局部控制取消固定 64x48 和地面像素距离假设。
- [x] 硬件软件接口：UGV 启动接线、真实 radiometric 温度输入及录包回放路径；仅声明软件支持，不声明实机已验证。
- [x] GP-UCB 强基线可运行；保留原策略，提供小规模对比与消融入口。
- [x] 纯算法测试、ROS 构建、代表性 A/B 闭环、慢层关闭/异常回退、运行耗时、输出数据与图表。
- [x] 用户文档、迁移说明、需求逐项证据审计与任务提交。

逐项证据与边界：[软件交付审计](../../software/software_completion_audit.md)。173 项测试通过，接口包及 7 个运行包构建成功；7 个缩减闭环通过记录、UGV 合成 ROS 输入验证、源码/install 56 对一致性检查均已留存。这里的勾选仅表示负责人授权的软件层验收，不表示原论文级门槛或实机验证通过。

## 执行顺序

1. 快层与慢层纯算法、接口及测试。
2. B 级几何渲染、观测适配与测试。
3. ROS 接线、规划/清场/局部控制、硬件输入适配。
4. 构建和代表性闭环，修复后补充审计与说明。

所有运行参数进入 YAML。增加的算法必须接入实际运行，不以占位实现或仅有单测代替完成。选择默认源簇方案，不另实现仅作为备选的变维粒子滤波。Pi 5 实际耗时和真实温度标定等待用户实机验证。
